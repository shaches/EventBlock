package oneblock;

/**
 * Enumeration of task types that a player can complete to progress through a level. Extensible for
 * future event types.
 */
public enum TaskType {
  BREAK,
  KILL,
  CRAFT,
  PLACE,
  FISH,
  ;
}
